services:
  kick-output:
    network_mode: "container:gluetun-torrent"
    environment:
      LOCAL_HLS_URL: "${LOCAL_HLS_URL_GLUETUN:-http://host.docker.internal:${HLS_ORIGIN_PORT:-8080}/live/index.m3u8}"
